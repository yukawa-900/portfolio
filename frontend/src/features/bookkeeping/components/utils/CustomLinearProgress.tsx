import React from "react";
import LinearProgress from "@material-ui/core/LinearProgress";

const CustomLinearProgress = () => {
  return <LinearProgress style={{ margin: "10px auto", width: 400 }} />;
};

export default CustomLinearProgress;
